{ parameter (pair address (ticket bytes)) ;
  storage (map address (ticket bytes)) ;
  code { UNPAIR ;
         UNPAIR ;
         SWAP ;
         READ_TICKET ;
         DROP ;
         DIG 2 ;
         SWAP ;
         SOME ;
         DIG 2 ;
         UPDATE ;
         NIL operation ;
         PAIR } }

